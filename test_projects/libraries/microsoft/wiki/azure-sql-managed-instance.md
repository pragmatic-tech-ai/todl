# Azure SQL Managed Instance

## Purpose

Fully managed SQL Server instance in Azure with near-complete compatibility with on-premises SQL Server. Native virtual network integration and lift-and-shift migration support for workloads that need instance-level features (Agent, cross-database queries) not in Azure SQL Database.

## Trade-offs

<!-- agent: this section is high-judgment; please fill in. -->
- 
- 
- 
