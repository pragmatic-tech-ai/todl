# AWS Panorama

## Purpose

The same Panorama CV-at-edge offering as `amazon-panorama` (the discovery report lists it twice — once under Amazon, once under AWS). This entry captures the AWS-branded listing of the appliance plus SDK.

## Trade-offs

- Same product as [`amazon-panorama`](amazon-panorama.md);
  duplicate entry exists because the discovery report saw two
  vendor pages for the same service. Trade-offs are identical
  — see the canonical entry.
- Use one slug per architecture diagram, not both. If both
  appear, that's a sign of a stale model carried through a
  re-import.
- The two slugs should collapse to one in a future library
  cleanup pass. Today the duplication is intentional to
  preserve label fidelity from the discovery report.
