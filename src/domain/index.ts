// Public surface of the Domain module (SPEC-06).
export {
    Domain,
    Heap,
    type PackageRef,
    type DomainToken,
    type DomainEdge,
    type SeedGraph,
    type ResolvedPackage,
    type PackageSource,
    type ResolveRequest,
} from "./domain.js";
