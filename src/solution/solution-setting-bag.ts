// Placeholder — the live setting-bag type. Fleshed out in a later task
// (SettingBagDefinition-backed values + write-through). Declared now so
// SolutionSession can type its SettingBags collection without a forward-dep.
export class SolutionSettingBag {}
