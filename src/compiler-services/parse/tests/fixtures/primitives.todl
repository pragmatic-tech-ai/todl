namespace adl.metaModels.bpmn.primitives
{
    primitive Identifier : string
    {
        description = "Stable kebab-case identifier.";
        regex       = "^[a-z][a-z0-9]*(?:-[a-z0-9]+)*$";
    }
}
